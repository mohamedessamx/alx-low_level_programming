#include "main.h"
/**
 * print_chessboard - function that prints the chessboard.
 * @a: gg
 * Return: nothing
 */
void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < 0; i++)
	{
		for (j = 0; j < 0; j++)
		{
			printf("%c", a[i][j]);
		}
		printf("\n");
	}
}
